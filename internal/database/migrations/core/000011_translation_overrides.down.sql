-- =====================================================
-- CORE SCHEMA - TRANSLATION OVERRIDES (rollback)
-- =====================================================

DROP TABLE IF EXISTS core.translation_overrides CASCADE;
